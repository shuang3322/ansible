#！/usr/bin/expect                    
set passwd [lindex $argv 0]   
spawn mysql_config_editor set --login-path=mysql_root --user=root --password
expect {
    "*password" {exp_send  "$passwd\r"}
}
expect eof
